version: ~> 1.0
language: python
python: 3.7
os: linux
dist: bionic
jobs:
  - python: 3.7
  - python: 3.8
cache: pip
before_install:
  - python --version
install:
  - pip install "poetry<2,>=1.0" tox
script:
  - tox
  - poetry install -v && poetry run pytest -s -v tests
