dist: xenial
language: python
python:
  - "3.7"
env:
  - TOXENV=flake8
  - TOXENV=format
  - TOXENV=mypy
  - TOXENV=py37
before_install:
  - python --version
install:
  - pip install tox
script:
  - travis_wait 60 tox
