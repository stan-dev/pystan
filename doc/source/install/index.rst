============
Installation
============

At the command line::

    $ python3 -m pip install pystan

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pystan
    $ python3 -m pip install pystan
